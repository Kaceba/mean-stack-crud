# MongoDB Configuration
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=password123
MONGO_DATABASE=mean-course

# MongoDB Connection String (used by backend)
MONGODB_URI=mongodb://admin:password123@mongodb:27017/mean-course?authSource=admin

# Backend Configuration
NODE_ENV=development
PORT=3000

# IMPORTANT: Copy this file to .env and change the credentials
# DO NOT commit .env to version control
#
# To get started:
# cp .env.example .env
#
# Then edit .env and change:
# - MONGO_INITDB_ROOT_PASSWORD (use a strong password)
# - MONGODB_URI (update with your new password)
